/**
 * React adapter for midi2musicxml.
 *
 * Import from this subpath to use the React hook and Web Worker integration:
 *   import { useMidi2MusicXMLWorker } from 'midi2musicxml/react';
 *
 * The core conversion API (no React dependency) is available at:
 *   import { midi2MusicXML } from 'midi2musicxml';
 */
export { useMidi2MusicXMLWorker } from './useMidi2MusicXMLWorker';
